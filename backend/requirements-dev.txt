# Install with: pip install -r requirements-dev.txt
# (requirements.txt is still required first)

-r requirements.txt
pytest==8.3.2
pytest-asyncio==0.24.0
httpx==0.27.2
pytest-cov==5.0.0
ruff==0.6.9
ruff==0.6.9
